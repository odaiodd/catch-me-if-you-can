

class Client:

    def __init__(self, id, screen_id, ads_id, encoding, time, img):
        self.id = id
        self.screen_id = screen_id
        self.ads_id = ads_id
        self.encoding = list(encoding)
        self.time = time
        self.img = img

